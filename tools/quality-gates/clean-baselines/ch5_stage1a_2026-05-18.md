# Stage 1a Invariant-Gate Clean Baseline — Ch 5 (The Accountability Gap)

**Date:** 2026-05-18
**Scope:** `manuscript/chapters/Chapter__5_THEACCOUNTABILITYGAP__Draft.md`
**Base sha:** `bc02767` (origin/main at retrofit-branch start; worktree branch `claude/ch5-pipeline-retrofit-serene-hoover-c5b37d`)
**Scaffolding-scan registry version:** `c41ae4a` (`tools/quality-gates/scaffolding-patterns.yaml`)
**Regressed-pattern-scan registry version:** `85878d1` (`tools/quality-gates/regressed-patterns.yaml`)
**Command:** `tools/scripts/check-corpus-invariants.sh --scope manuscript/chapters/Chapter__5_THEACCOUNTABILITYGAP__Draft.md --verbose`
**Pipeline doctrine reference:** [`tools/pipeline-doctrine/commons_bonds_pipeline_doctrine_stage_1_v1.0.0.md`](../../pipeline-doctrine/commons_bonds_pipeline_doctrine_stage_1_v1.0.0.md) §1

---

## Scan results

| Severity | Count |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| **Total** | **0** |

Patterns scanned: 78 (scaffolding + regressed combined).
Files scanned: 1.
Exit code: 0.
Scanner output: `[corpus-invariants] CLEAN BASELINE — no matches across 78 patterns x 1 files`.

## §HIGH

None.

## §MEDIUM

None.

## §LOW

None.

## Allowlist updates committed

None — clean scan, no allowlist amendments needed.

Note on the per-file allowlist already in place (per Ch 5 retrofit handoff stub §1, line 20): the handoff predicts the "settlement-ratified" usage at line 48 ("the documented, court-validated, settlement-ratified empirical magnitude of the accountability gap") is allowlisted in the regressed-patterns registry. The scanner returned zero matches with no allowlist-suppression telemetry, so either (a) the registry's `ratified` pattern does not currently match the compound form `settlement-ratified`, or (b) the allowlist is not yet codified and the scan's clean exit reflects pattern-non-match rather than allowlist-suppression. Either way the verdict stands; if a future tightening of the `ratified` regex matches the compound, the allowlist should be codified at that point with rationale referencing this baseline.

## Verdict

**CLEAN BASELINE — Stage 1a complete; ready for Stage 1c.**

Confirms the 2026-05-17 leak-check survey expectation (per pipeline doctrine §1.5 retrofit-mode notes) that retrofit Stage 1a fires clean against existing chapter sources. The prior Pass 1 + Pass 2 + Pass 3 application sessions (Phase C-α + Phase C-β + Phase C-β-follow-up commit chains, terminating at `d2ee178`) closed without introducing scaffolding-vocabulary leakage; the regressed-pattern registry's current pattern set does not match any deprecated phrasing in the chapter as it currently stands. Result tracks the Ch 1 retrofit baseline (commit `3582823`, 2026-05-18) — second of the 4 standardization-comparison-bed retrofits to clear Stage 1a clean.
