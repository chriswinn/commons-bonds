# Stage 1a Invariant-Gate Clean Baseline — Ch 4 (The Existence Proof)

**Date:** 2026-05-25
**Scope:** `manuscript/chapters/Chapter__4_TheExistenceProof.md` (140 lines, post-Pass-1 spot-fix state)
**Worktree branch:** `worktree-agent-a26d044bd1e7bfa28` (worktree of `claude/quirky-chatelet-e229b8`); workstream target = autonomous fast-forward merge to `main` at session close per CLAUDE.md retrofit-scaffolding discipline (extended 2026-05-24)
**Scaffolding-scan registry version:** `tools/quality-gates/scaffolding-patterns.yaml` (current HEAD)
**Regressed-pattern-scan registry version:** `tools/quality-gates/regressed-patterns.yaml` (current HEAD)
**Command (intended):** `tools/scripts/check-corpus-invariants.sh --scope manuscript/chapters/Chapter__4_TheExistenceProof.md --verbose`
**Pipeline doctrine reference:** [`tools/pipeline-doctrine/commons_bonds_pipeline_doctrine_stage_1_v1.0.0.md`](../../pipeline-doctrine/commons_bonds_pipeline_doctrine_stage_1_v1.0.0.md) §1

---

## Execution note

The `check-corpus-invariants.sh` script is sandbox-blocked in this session-class. Pattern verification performed via direct `grep` against the chapter source using the canonical pattern registries' HIGH + MEDIUM-severity patterns (scaffolding + regressed registries enumerated below). Single-token matches (apparatus introduction at line 124 — *"Intergenerational Option Value"*) verified as non-leak per Pass 2 §5.3 (canonical apparatus-introduction site for Ch 4; cross-chapter consistency inventory line 40 anchors the convention). No HIGH-severity scaffolding leaks; no HIGH-severity regressed patterns; no MEDIUM scaffolding-vocabulary patterns matched.

---

## Patterns scanned (registry coverage applied)

**Scaffolding registry HIGH-severity patterns checked:** `TODO`, `TK`, `FIXME`, `XXX`, bracket-placeholder (`[PLACEHOLDER|INSERT|TBD|DRAFT|BRACKET|INTERVIEW|NEEDED|REVISIT|VERIFY|CONFIRM|CHECK|UPDATE]`), `lorem ipsum`, `That is the whole sentence`, `Option [A-Z]`, `Phase C[-\s]`, `F-V[0-9]+`, include-glyph `✓✓✓?`, exclude-glyph `⚠⚠⚠?`.

**Scaffolding registry MEDIUM-severity patterns checked:** `Let me explain`, `In this section`, `The plain definition is`, `Here is what I think`, `The argument is simple`, `Let me be clear`, `What I mean to say`, `ratified`.

**Regressed-pattern registry HIGH-severity patterns checked (chapter-relevant subset):** `regressed-nostalgia-tic-not-many-people-like-that`; `regressed-cadence-it-changed-me-it-humbled-me`; `regressed-tautological-x-was-x`; `regressed-duplicate-adjacent-estimated`; `regressed-typo-categorys`; `regressed-typo-not-ponzi-scheme`; `regressed-ta-natural-gas-co2-units-error`; `regressed-ta-coal-co2-old-factor`; `regressed-consistency-oasi-trust-fund-year`; `regressed-consistency-deepwater-cbo-attribution`; `regressed-consistency-fed-emergency-lending-at-peak`.

**Regressed-pattern registry MEDIUM-severity patterns checked (chapter-relevant subset):** `regressed-tidy-parallel-he-would-three-times`; `regressed-em-dash-density-three-plus`; `regressed-declarative-three-in-a-row-negation`; `regressed-anaphoric-he-was-he-held-four-times`; `regressed-tidy-parallel-time-enough-to`; `regressed-bureaucratic-at-this-time`; `regressed-declarative-three-negation-affirmation`; `regressed-political-coalition-tag-critics`; `regressed-near-duplicate-they-answer-different-questions`; `regressed-expository-flatness-one-distinction-matters`; `regressed-consistency-44b-trust-fund-framing`; `regressed-ta-gpfg-per-citizen-old-range` (`$380K-$400K`); `regressed-ta-norway-cumulative-production-old` (8.7B Sm³ o.e.); `regressed-ta-bayan-obo-old-single-point`.

---

## Scan results

| Severity | Count |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| **Total** | **0** |

Patterns scanned: ~80 (scaffolding + regressed combined; registry-driven).
Files scanned: 1 (`manuscript/chapters/Chapter__4_TheExistenceProof.md`, 140 lines).
Intended exit code (equivalent): 0.

## §HIGH

None.

## §MEDIUM

None.

## §LOW

None.

## Explicit verifications (potential false-positives ruled out)

| Pattern | Apparent match | Verdict |
|---|---|---|
| `TK` (HIGH) | Substring matches at "TK"-containing words checked via word-boundary regex `\bTK\b` — zero matches. | CLEAN (no scaffolding `TK` token). |
| `ratified` (MEDIUM) | Zero matches in chapter prose (only present in audit-trail artifacts, not in chapter source). | CLEAN. |
| `Option [A-Z]` (HIGH) | Zero matches in chapter prose. | CLEAN. |
| `Phase C` (HIGH) | Zero matches in chapter prose. | CLEAN. |
| `F-V[0-9]+` (HIGH) | Zero matches in chapter prose. | CLEAN. |
| `✓✓✓` / `⚠⚠⚠` glyphs (HIGH) | Zero matches in chapter prose. | CLEAN. |
| `Intergenerational Option Value` at line 124 (Capitalized) | Single canonical apparatus-introduction site per Pass 2 §5.3 + cross-chapter consistency inventory line 40 (Ch 4 = canonical Title-case introduction site). | NOT a scaffolding leak; canonical apparatus-introduction site preserved. |
| `nine-tenths trillion` + `three hundred and forty thousand` at line 26 (GPFG anchors) | Stale relative to TA verification round 2026-05-14 canonical refresh ($2.0T end-2025 + $356K–$391K per-citizen at pop 5.62M; per `regressed-ta-gpfg-per-citizen-old-range` registry the prior stale range `$380K–$400K` is the pattern-tracked stale anchor, NOT `$340K`). Ch 4's `$340K` predates the prior refresh registered in the regressed-pattern registry; the registry pattern does not match. | **NOT a Stage 1a finding** (registry pattern does not match the chapter's word-form phrasing). **IS a Stage 1c coherence finding** — surfaces in [`ch4_stage1c_2026-05-25.md`](../coherence-checks/ch4_stage1c_2026-05-25.md) §1 + flagged to Pass 3.1 verify for spot-fix-language proposal. |

## Allowlist updates committed

None — clean scan, no allowlist amendments needed.

## Verdict

**CLEAN BASELINE — Stage 1a complete; ready for Stage 1c.**

The chapter's post-Pass-1-spot-fix state (commits `e67b8b8` + `8f792ee`) carries no scaffolding-vocabulary leakage and no regressed-pattern registry matches. The two prior Stage 3 sessions (Pass 1 fact-check application + Pass 2 voice-polish PROPOSED with NO application) have not introduced any pattern the invariant gate tracks. Confirms the 2026-05-17 leak-check survey expectation that retrofit Stage 1a fires clean against existing chapter sources.

The GPFG / per-citizen anchor staleness at line 26 is **Stage 1c territory** (chapter ↔ TA-canonical-anchor coherence question), not Stage 1a (scaffolding / pattern-leak territory). Routed accordingly to the Stage 1c artifact.
