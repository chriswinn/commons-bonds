# PASTE-TEXT → IPG-reframe session (AFTER Session E's Deepwater repro)

*(Copy below the line into the target session.)*

---

IPG "canonical/documented" reframe — DO AFTER Session E's Deepwater repro lands, on the MERGED tree.

Context: The framework reports IPG (Intergenerational Pricing Gap) multiples - McDowell "33-122x canonical," "50-555x triangulated," §3.2 IPG=33, §16.4 33-122x - with "canonical/documented" framing that reads as an EXTERNAL-AUTHORITY claim it can't cash. These are framework-COMPUTED, not externally documented. Reframe to "framework-computed (per §11.x derivation)" and make each multiple trace to its RCV/price work.

HARD DEPENDENCIES (satisfy first):
1. Session E's Deepwater repro fix must land - the relabel asserts "framework-computed per derivation," so the shown derivation must actually reproduce. Don't relabel a number whose derivation doesn't reproduce.
2. Do it on the MERGED tree, after M3's §9.5/§11.11 cascade settles - M3 may have moved those numbers. Reframing against a stale §9.5 just creates another divergent §9.5.

INSTANCES (verify line numbers on the merged tree - these are from the notation-sweep branch):
- §3.2 IPG=33 (~762)
- §9.5 three-model table (3148, 4507) + the convergence narrative
- §11.6 "roughly consistent with the canonical 33-122x range" (4903); "50x IPG ... empirically grounded by triangulation" (4910)
- §11.9 IPG 33-122x (3892)
- §11.11 "33-122x canonical -> 50-555x triangulated" (6203, 6206, 6220, 6241, 6242)
- §14.3 "McDowell County IPG range of 33-122x" (6573)
- §16.4 33-122x (sensitivity table)

ALSO FOLD IN (subsumes correctness item #3 / B6): the §9.5 convergence-NARRATIVE ordering claim ("Real Options systematically lowest / RCV highest") contradicts the table. Reconcile it to the FINAL (post-M3) §9.5 numbers in the same pass. (Against the pre-M3 table it failed in 2 of 4 cases - Real Options held the highest upper bound in Deepwater 21x and Baotou 48x; re-verify against the final table.)

Reframe pattern: "the canonical 33-122x range" -> "the framework-computed 33-122x range (RCV / market price per §11.6 derivation)"; "documented" -> "computed"; ensure each multiple shows or cross-refs its RCV/price arithmetic.

Verify independently; do it once on the merged tree after both dependencies clear.
