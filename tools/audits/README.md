# tools/audits/ — strategic-snapshot audits + standing inventories

One-shot or standing-reference audit artifacts that are CROSS-cutting (not per-essay, not
per-chapter rigor — those live in `publishing/essays/<venue>/rigor/` and `tools/rigor-passes/`).

Includes (2026-06-10): corpus primary-source register, TA input-provenance audits,
back-matter scaffolding-leak audit, and the early framework-era chapter/case-study audits
relocated from the misleadingly-named `core/chapters/` + `core/case-studies/` (2026-06-10;
those directories held AUDITS, never content — see `MAP.md`).
